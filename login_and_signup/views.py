from django.shortcuts import render

# Create your views here.

#TODO
# Add this code to when a user signs up

# from profile_page.models import User
# user = User(username = request.user.username, first_name = "John", last_name = "Doe", weight = 0, age = 0, height = 0)
# user.save()